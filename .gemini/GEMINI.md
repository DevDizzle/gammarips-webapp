## Gemini Added Memories
- The user requires Next.js version 15.0.5 for the profitscout-app project to avoid dependency issues.
- The application is currently in 'Growth Mode'. FREE_MODE is set to true in config.ts. The onboarding flow is optimized for frictionless signup: the homepage CTA links directly to the public dashboard, which offers a sneak peek (top 3 items) and gates the rest/details behind a signup prompt. This strategy aims to build the user base to ~500 daily users before re-enabling payments.
- The ProfitScout MCP Cloud Run server (profitscout-mcp-hrhjaecvhq-uc.a.run.app) returns HTTP 421 with body 'Invalid Host header' even when valid Host headers are sent via HTTP/1.1. This indicates a strict server-side AllowedHosts middleware issue.
- The user's GEMINI_API_KEY was found to be invalid (started with AQ.Ab instead of AIza), causing 'API keys are not supported' errors in Genkit. Also fixed references to non-existent 'gemini-2.5-flash-lite' model.
- The ProfitScout agent has been refactored to use a pure MCP architecture with dynamic tool discovery. The 'chatRouterFlow' now routes all requests to 'profitScoutAgent'. Legacy flows 'groundedQaFlow' and 'customerServiceFlow' are deprecated but still present. Next steps involve fine-tuning the financial analyst persona and verifying customer service interactions.
- Fixed MCP client connection stability in `src/lib/mcp-client.ts` by adding auto-reset on disconnection and retry logic in `src/ai/agents/profit-scout-agent.ts`. This resolves issues where the AI agent lost access to tools on subsequent turns.
- The ProfitScout Cloud Run server code (Echo-Trigger) seems to be missing from the workspace, specifically the python function 'analyze_news_batch' requested for debugging.
- The webapp now has public SEO report pages under '/reports' and '/reports/[date]', driven by Firestore 'overnight_summaries' and 'overnight_signals' collections. These pages are server-rendered and indexable.
- Implemented email capture flow (frontend + backend) and drip sequence Cloud Function logic in Echo-Trigger/functions/main.py.
- Deployed Overnight Edge update to main branch. Includes email capture, reports, and new signal dashboard.
- The GammaRips MCP API is now free, requires no authentication, and the developers page is located at /developers.
