# REPLAN 2026-07-17 — Video-led landing redo (this repo's slice)

Master plan: `gammarips-engine/docs/EXEC-PLANS/2026-07-17-simplification-replan.md`
(RATIFIED). Owner decisions: video hosted on **YouTube**; ship gated on the video
existing. `main` auto-deploys — ALL work on a `landing/*` branch, land via PR, /ship
gate (content-reviewer + claim-skeptic) before merge.

## Steps
1. **Hero redo** (`src/components/landing/hero.tsx`, composed in `src/app/page.tsx`):
   YouTube embed slot (poster image until the asset exists) + one line of copy —
   "I trade my own tool every morning. $39/mo gets your agent the same data." — + the
   existing two CTAs (/developers, /signals). No CSP exists in this repo; nothing blocks
   the embed. Do NOT self-host video bytes (apphosting maxInstances: 1).
   Framing/compliance: personal process, NOT a signal to follow; same disclaimer
   treatment as `agent-demo.tsx`; keep the FAQ history of the retired $39-pick tier.
2. **Keep**: curation-funnel, Today's Market Snapshot, pool cards, honesty section, FAQ.
   Tighten everything else per the 07-08 copy rule (8th-grade sentences, every number
   gets one plain sentence, NO em dashes in public copy).
3. **Constants**: add `TOOL_COUNT` and `PRICE_MONTHLY` to `src/lib/` and replace the
   hardcoded literals. "23 tools" sites: page.tsx:27, agent-demo.tsx:62, faq.tsx:15,19,
   developers/page.tsx (7 sites), pricing/page.tsx:23, pricing-client.tsx:17,
   account/page.tsx:166, about/page.tsx:140, mailgun.ts:107,162,298, public/llms.txt,
   public/mcp.json, public/skill.md, public/.well-known/ai-plugin.json. "$39" sites:
   pricing-client, developers, pricing, about, disclosures, terms, account, faq,
   mailgun, the four public/ files. (Server truth is 29 today, 9 after MCP v4 — the
   constant makes the next change a one-liner.)
4. **v4 surface docs** (AFTER gammarips-mcp v4 deploy freezes names — see that repo's
   REPLAN file for the 9-tool map): rewrite /developers tool catalog + the four public/
   agent-discovery files to the 9 tools.
5. **Ship**: video URL lands in the hero → /ship gate → PR merge (= production).

DoD: PR merged, video live, every tool-count/price string resolves from the constants.

## Checklist
- [x] landing/* branch + hero with poster slot
- [x] copy tighten pass (07-08 rule)
- [x] TOOL_COUNT/PRICE constants replacing all literals
- [ ] /developers + public/ files on v4 surface
- [ ] video embedded (YouTube) + /ship gate + merge
